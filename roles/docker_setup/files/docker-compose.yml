# docker-compose.yml
# ───────────────────────────────────────────────────────
# Initial stub file for service orchestration.
# Add MongoDB, backend, and frontend containers step-by-step.
# ───────────────────────────────────────────────────────


# docker-compose.yml
# ────────────────────────────────────────────────
# Stubbed Compose file for IP3 container orchestration.
# No services yet—just a placeholder for MongoDB, backend, and frontend.
# ────────────────────────────────────────────────

version: '3.8'
services:
  
  # mongo:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - ./mongo_data:/data/db
  #   restart: always

  # backend:
  #   build: ./backend
  #   ports:
  #     - "5000:5000"
  #   depends_on:
  #     - mongo

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
